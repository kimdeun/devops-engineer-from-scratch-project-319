apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: bulletin-board-prod
spec:
  selector:
    app: bulletin-board
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30080
  type: LoadBalancer
